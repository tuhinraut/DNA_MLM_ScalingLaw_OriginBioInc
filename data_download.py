"""
Data download and preprocessing script.

TODO: Implement this script.

This script should:
  1. Download a dataset of protein-coding DNA sequences (CDS).
  2. Clean / filter the raw data as needed.
  3. Save the processed sequences in a format that load_sequences()
     in dataset.py can read.

The output file path should be printed or configurable so that
the orchestration script can pass it to train.py via --data_path.
"""
